package io.github.mgluiz.algorithms;

public enum Algorithms {
    // Symmetric (Private Key)
    AES,
    DES,

    // Asymmetric (Public Key)
    RSA,
    ECC,

    // HASH (No Key)
    SHA256,
    MD5,
}
